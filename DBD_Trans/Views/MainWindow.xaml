<Window x:Class="DBD_Trans.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:DBD_Trans.Views"
        xmlns:viewModels="clr-namespace:DBD_Trans.ViewModels"
        xmlns:converters="clr-namespace:DBD_Trans.Converters"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="Локализация DBD" Height="600" Width="950"
        WindowState="Maximized"
        Background="#FF171717"
        FocusVisualStyle="{x:Null}">
    <Window.Resources>
        <converters:BoolInverseConverter x:Key="BoolInverseConverter" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <!-- Цветовая палитра -->
        <SolidColorBrush x:Key="BackgroundDark" Color="#FF171717"/>
        <SolidColorBrush x:Key="BackgroundMedium" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="BackgroundLight" Color="#FF212121"/>
        <SolidColorBrush x:Key="ForegroundPrimary" Color="#FFE0E0E0"/>
        <SolidColorBrush x:Key="ForegroundSecondary" Color="#FFA0A0A0"/>
        <SolidColorBrush x:Key="WarningOrange" Color="#FFCA5100"/>
        <SolidColorBrush x:Key="WarningOrangeLight" Color="#33CA5100"/>
        <SolidColorBrush x:Key="BorderDark" Color="#FF333333"/>
        <SolidColorBrush x:Key="CompletedRowBackground" Color="#FF232E48"/>
        <SolidColorBrush x:Key="ErrorRed" Color="#FF3D1C1C"/>
        <SolidColorBrush x:Key="WarningYellowLight" Color="#FF504A28"/>

        <!-- Стиль для ComboBox (Темная тема) -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource BackgroundMedium}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDark}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Focusable="False"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" 
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="3">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter x:Name="ContentSite"
                                                                    Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                    ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                    TextBlock.Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                                                    HorizontalAlignment="Left"
                                                                    VerticalAlignment="Center"
                                                                    Margin="6,0,0,0"/>
                                                <Path x:Name="Arrow" 
                                                      Grid.Column="1" 
                                                      Fill="{StaticResource ForegroundSecondary}" 
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center" 
                                                      Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="Popup" 
                                   Placement="Bottom" 
                                   IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   AllowsTransparency="True" 
                                   Focusable="False" 
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown" 
                                      SnapsToDevicePixels="True" 
                                      MinWidth="{TemplateBinding ActualWidth}" 
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" 
                                            Background="{StaticResource BackgroundMedium}" 
                                            BorderBrush="{StaticResource BorderDark}" 
                                            BorderThickness="1" 
                                            CornerRadius="3"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для элементов ComboBox -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="3"
                                Margin="2,1">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource WarningOrange}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Остальные стили (загрузка, TextBox, ScrollBar, DataGrid и т.д.) -->
        <Style x:Key="ThreeDotsLoading" TargetType="ItemsControl">
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Ellipse Width="35" Height="35" Fill="{StaticResource WarningOrange}" Margin="8.5,0">
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0.3" To="1.0" Duration="0:0:0.6" AutoReverse="True"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BackgroundMedium}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDark}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="CaretBrush" Value="{StaticResource ForegroundPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarButtonStyle" TargetType="RepeatButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="{StaticResource WarningOrange}" CornerRadius="6" Margin="2,0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="{StaticResource BackgroundMedium}"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="6"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Style="{StaticResource ScrollBarButtonStyle}"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbStyle}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Style="{StaticResource ScrollBarButtonStyle}"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль DataGrid (без рамок фокуса и внешних обводок) -->
        <Style TargetType="DataGrid">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Background" Value="{StaticResource BackgroundMedium}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="{StaticResource BackgroundMedium}"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource BackgroundLight}"/>
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderDark}"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="SelectionMode" Value="Extended"/>
            <Setter Property="CanUserResizeColumns" Value="True"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="CanUserSortColumns" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGrid">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="DG_ScrollViewer" Focusable="False">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
        
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Background" Value="{StaticResource BackgroundDark}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDark}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" RecognizesAccessKey="True"/>
                                <Thumb x:Name="PART_RightHeaderGripper" HorizontalAlignment="Right" Width="2" Background="Transparent" Cursor="SizeWE"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource BackgroundMedium}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDark}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="0" CornerRadius="3" Margin="2,1">
                            <Grid>
                                <ContentPresenter x:Name="ContentPresenter" Content="{TemplateBinding Header}" RecognizesAccessKey="True" Margin="{TemplateBinding Padding}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource WarningOrange}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource ForegroundSecondary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Height" Value="1"/>
            <Setter Property="Background" Value="{StaticResource BorderDark}"/>
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Background="{TemplateBinding Background}" Height="{TemplateBinding Height}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Background" Value="{StaticResource BackgroundMedium}"/>
            <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridRow">
                        <Border x:Name="RowBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <SelectiveScrollingGrid>
                                <DataGridCellsPresenter ItemsPanel="{TemplateBinding ItemsPanel}"/>
                            </SelectiveScrollingGrid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                    <Setter Property="Background" Value="{StaticResource BackgroundLight}"/>
                </Trigger>

                <!-- НЕТ ПЕРЕВОДА -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding HasTranslation}" Value="False"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=(ItemsControl.AlternationIndex)}" Value="0"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="False"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="False"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#33CA5100"/>
                    <Setter Property="BorderBrush" Value="{StaticResource WarningOrange}"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding HasTranslation}" Value="False"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=(ItemsControl.AlternationIndex)}" Value="1"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="False"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="False"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#4CCA5100"/>
                    <Setter Property="BorderBrush" Value="{StaticResource WarningOrange}"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding HasTranslation}" Value="False"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#66CA5100"/>
                    <Setter Property="BorderBrush" Value="{StaticResource WarningOrange}"/>
                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                    <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding HasTranslation}" Value="False"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#80CA5100"/>
                    <Setter Property="BorderBrush" Value="{StaticResource WarningOrange}"/>
                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                    <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
                </MultiDataTrigger>

                <!-- ЗАВЕРШЕНО БЕЗ ОШИБОК -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="Completed"/>
                        <Condition Binding="{Binding HasErrors}" Value="False"/>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=(ItemsControl.AlternationIndex)}" Value="0"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="False"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="False"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FF232E48"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="Completed"/>
                        <Condition Binding="{Binding HasErrors}" Value="False"/>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=(ItemsControl.AlternationIndex)}" Value="1"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="False"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="False"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FF1F2942"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="Completed"/>
                        <Condition Binding="{Binding HasErrors}" Value="False"/>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FF1C253B"/>
                    <Setter Property="BorderBrush" Value="#FF0239C3"/>
                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                    <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="Completed"/>
                        <Condition Binding="{Binding HasErrors}" Value="False"/>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FF1C253B"/>
                    <Setter Property="BorderBrush" Value="#FF0239C3"/>
                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                    <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
                </MultiDataTrigger>

                <!-- ЗАВЕРШЕНО С ОШИБКАМИ -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="Completed"/>
                        <Condition Binding="{Binding HasErrors}" Value="True"/>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=(ItemsControl.AlternationIndex)}" Value="0"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="False"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="False"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FF3D1C1C"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="Completed"/>
                        <Condition Binding="{Binding HasErrors}" Value="True"/>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=(ItemsControl.AlternationIndex)}" Value="1"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="False"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="False"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FF361919"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="Completed"/>
                        <Condition Binding="{Binding HasErrors}" Value="True"/>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FF351616"/>
                    <Setter Property="BorderBrush" Value="#FFC10101"/>
                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                    <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="Completed"/>
                        <Condition Binding="{Binding HasErrors}" Value="True"/>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FF351616"/>
                    <Setter Property="BorderBrush" Value="#FFC10101"/>
                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                    <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
                </MultiDataTrigger>

                <!-- В ПРОЦЕССЕ С ОШИБКАМИ -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="InProgress"/>
                        <Condition Binding="{Binding HasErrors}" Value="True"/>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=(ItemsControl.AlternationIndex)}" Value="0"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="False"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="False"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FF504A28"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="InProgress"/>
                        <Condition Binding="{Binding HasErrors}" Value="True"/>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=(ItemsControl.AlternationIndex)}" Value="1"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="False"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="False"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FF453F20"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="InProgress"/>
                        <Condition Binding="{Binding HasErrors}" Value="True"/>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FF3A3410"/>
                    <Setter Property="BorderBrush" Value="#FFC1A203"/>
                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                    <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Status}" Value="InProgress"/>
                        <Condition Binding="{Binding HasErrors}" Value="True"/>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#FF3A3410"/>
                    <Setter Property="BorderBrush" Value="#FFC1A203"/>
                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                    <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
                </MultiDataTrigger>

                <!-- ОБЫЧНЫЕ СТРОКИ -->
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding HasErrors}" Value="False"/>
                        <Condition Binding="{Binding Status}" Value="InProgress"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#2A0F0600"/>
                    <Setter Property="BorderBrush" Value="{StaticResource WarningOrange}"/>
                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                    <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding HasTranslation}" Value="True"/>
                        <Condition Binding="{Binding HasErrors}" Value="False"/>
                        <Condition Binding="{Binding Status}" Value="InProgress"/>
                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="True"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="#2A0F0600"/>
                    <Setter Property="BorderBrush" Value="{StaticResource WarningOrange}"/>
                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                    <Setter Property="Foreground" Value="{StaticResource ForegroundPrimary}"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid Margin="15" IsEnabled="{Binding IsLoading, Converter={StaticResource BoolInverseConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Панель поиска и фильтра -->
            <Border Grid.Row="0" Background="{StaticResource BackgroundMedium}" CornerRadius="4" Padding="10" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔍" Foreground="{StaticResource ForegroundSecondary}" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14"/>
                    <TextBlock Text="Поиск:" Foreground="{StaticResource ForegroundSecondary}" VerticalAlignment="Center" Margin="0,0,8,0"/>

                    <!-- ДОБАВЛЕНО Height="30" -->
                    <TextBox x:Name="SearchBox" Width="350" Height="30" VerticalAlignment="Center" TextChanged="SearchBox_TextChanged"
                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBlock Text="Фильтр:" Foreground="{StaticResource ForegroundSecondary}" VerticalAlignment="Center" Margin="15,0,8,0"/>

                    <!-- ДОБАВЛЕНО Height="30" -->
                    <ComboBox x:Name="FilterComboBox" Width="220" Height="30" VerticalAlignment="Center"
                  ItemsSource="{Binding FilterOptions}"
                  SelectedItem="{Binding SelectedFilter}"
                  SelectionChanged="FilterComboBox_SelectionChanged"/>

                    <TextBlock Text="{Binding ResultCountText}" Foreground="{StaticResource ForegroundSecondary}"
                   VerticalAlignment="Center" Margin="15,0,0,0" FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- Таблица -->
            <DataGrid x:Name="LocalizationGrid" Grid.Row="1"
                          ItemsSource="{Binding FilteredEntries}"
                          SelectedItem="{Binding SelectedEntry}"
          
                            VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      VirtualizingPanel.ScrollUnit="Pixel"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                            VirtualizingPanel.CacheLengthUnit="Page"
                      VirtualizingPanel.CacheLength="2,1">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Анализ" Command="{Binding AnalyzeCommand}" CommandParameter="{Binding SelectedEntry}">
                            <MenuItem.Icon>
                                <Path Data="M0,0 L10,10 M10,0 L0,10" Stroke="{StaticResource ForegroundPrimary}" StrokeThickness="2"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Перейти" Command="{Binding GoToCommand}" CommandParameter="{Binding SelectedEntry}">
                            <MenuItem.Icon>
                                <Path Data="M0,0 L8,5 L0,10 Z" Fill="{StaticResource ForegroundPrimary}" Width="12" Height="12" Stretch="Uniform" Margin="0,0,4,0"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </DataGrid.ContextMenu>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="№" Binding="{Binding Index}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Right"/>
                                <Setter Property="Foreground" Value="{StaticResource ForegroundSecondary}"/>
                                <Setter Property="Margin" Value="0,0,5,0"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Код строки" Binding="{Binding Key}" Width="260">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontFamily" Value="Consolas, Courier New"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="TextWrapping" Value="NoWrap"/>
                                <Setter Property="ToolTip" Value="{Binding Key}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Русский" Binding="{Binding Russian}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextWrapping" Value="Wrap"/>
                                <Setter Property="LineHeight" Value="20"/>
                                <Setter Property="MaxHeight" Value="200"/>
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Английский" Binding="{Binding English}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextWrapping" Value="Wrap"/>
                                <Setter Property="LineHeight" Value="20"/>
                                <Setter Property="MaxHeight" Value="200"/>
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Статусная строка -->
            <Border Grid.Row="2" Background="{StaticResource BackgroundMedium}" CornerRadius="4" Padding="10,6" Margin="0,10,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Всего строк: " Foreground="{StaticResource ForegroundSecondary}"/>
                    <TextBlock Text="{Binding TotalCount}" Foreground="{StaticResource ForegroundPrimary}" FontWeight="Bold"/>
                    <TextBlock Text=" | Отображено: " Foreground="{StaticResource ForegroundSecondary}" Margin="10,0,0,0"/>
                    <TextBlock Text="{Binding DisplayedCount}" Foreground="{StaticResource ForegroundPrimary}" FontWeight="Bold"/>
                    <TextBlock Text=" | Без перевода: " Foreground="{StaticResource ForegroundSecondary}" Margin="10,0,0,0"/>
                    <TextBlock Text="{Binding MissingCount}" Foreground="{StaticResource ForegroundPrimary}" FontWeight="Bold"/>
                    <TextBlock Text=" | Завершено: " Foreground="{StaticResource ForegroundSecondary}" Margin="10,0,0,0"/>
                    <TextBlock Text="{Binding CompletedCount}" Foreground="{StaticResource ForegroundPrimary}" FontWeight="Bold"/>
                    <TextBlock Text=" | Замечаний: " Foreground="{StaticResource ForegroundSecondary}" Margin="10,0,0,0"/>
                    <TextBlock Text="{Binding TotalErrorCount}" Foreground="{StaticResource ForegroundPrimary}" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Экран загрузки -->
        <Border Background="#CC1E1E1E" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ItemsControl Style="{StaticResource ThreeDotsLoading}">
                        <sys:Int32>1</sys:Int32>
                        <sys:Int32>2</sys:Int32>
                        <sys:Int32>3</sys:Int32>
                    </ItemsControl>
                    <TextBlock Text="Загрузка данных..." Foreground="#FFE0E0E0" FontSize="14" FontWeight="SemiBold" Margin="0,20,0,0" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>